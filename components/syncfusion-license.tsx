'use client';

// CRITICAL: This must register the license BEFORE any Syncfusion components are rendered
// Import and execute the license registration immediately when this module loads
import '@/lib/syncfusion-license';

export default function SyncfusionLicense() {
  // The license registration happens when the module loads (above import)
  // This component itself doesn't need to do anything
  return null;
}
